{
	"version": 2,
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "src/(.*)",
			"dest": "src/dist/index.js"
		}
	],
	"rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
